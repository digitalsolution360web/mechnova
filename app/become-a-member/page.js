import BecomeMemberClient from "./BecomeMemberClient";

export const metadata = {
  title: "Become a Member - Mechnova Machines",
  description: "Join Mechnova as a member. Register for updates, offers and community.",
};

export default function BecomeMemberPage() {
  return <BecomeMemberClient />;
}
